# 仓库协作规范

## 产品边界

- 保持本地优先，不添加云端部署作为默认流程。
- 不添加 MCP。智能体通过 CLI、HTTP API、`/llms.txt`、`/capabilities.json` 和路径级 `/skill.md` 使用服务。
- 未完成的能力必须标记为 `planned`，不能在界面或文档中表现成已经可用。

## 界面语言

- 默认界面语言为简体中文。
- 同一个界面、组件或句子只能使用一种自然语言，不要把中文标题与英文眉题、英文状态或英文说明混排。
- 品牌名、命令、路径、文件格式、计量单位和无法合理翻译的行业缩写可以保留原文；存在自然中文表达时优先使用中文。
- 语言选择器可以使用目标语言的自称，例如“中文”和“English”；选择后必须切换整套界面文案，不能只切换局部组件。
- 标签页、字段名、状态标签、空状态、按钮、工具提示和底部状态信息都必须遵守同一语言规则。
- 新增界面文案时，在提交前检查整个页面，不能只修正被指出的单个位置。

## 间距与内边距

- 使用 CSS 间距变量，不要在同层级组件上任意写不同数值。
- 面板边界到第一层内容盒的横向距离统一由 `--panel-edge-inset: 14px` 定义，`--panel-padding` 只能引用它，不能形成第二套基准。
- 一个方向上的边距只能有一个所有者。外层容器持有横向边距时，直接子组件不得再添加横向 `padding`；子组件持有时，外层容器的对应方向必须为零。
- 树形层级使用显式 `data-depth` 和 `--tree-indent-step` 计算，不通过嵌套容器的 `padding-left` 累加。新增深度时扩展层级映射，不能叠加新的包装间距。
- 同级图标、文字和图片之间使用 `gap`；不要用各自的 `margin` 拼出间距。图标使用统一尺寸的图标盒，图片先进入固定尺寸的媒体盒，再对齐内容基准。
- CSS 能保证边界到元素布局盒的距离；图标内部留白属于素材本身。需要像素级视觉一致时，只使用统一 `viewBox` 和光学留白规范的同一套图标资源。
- 表单控件统一使用 `--control-padding-y: 8px` 和 `--control-padding-x: 10px`。
- 同类型组件必须共享选择器或组件样式；不要通过单独覆盖制造 12px、14px、16px 等细小差异。
- 新增面板前先复用现有间距变量，并以右侧“特征图”内容区作为当前面板密度基准。
- 底部紧凑工具区的外层容器不添加内边距。菜单行使用固定 40px 行高负责纵向居中，只持有 14px 横向内容内缩；不要同时用不对称的横纵 `padding` 拼出控件高度。菜单行保留图标和文字，使用无边框样式，不做成卡片或大面积独立按钮。

## 文本与信息层级

- 标题、副标题、树节点名称和其他标识性短文本默认保持单行；可用宽度不足时使用省略号，承载文本的弹性项必须设置 `min-width: 0`。
- 状态标签、尾部操作和图标不得挤压标题区域；它们使用固定收缩策略，剩余空间全部交给标题文本。
- 纯说明文字默认直接放在所属内容区中，不自动添加边框、圆角或背景。
- 只有当内容具备独立选择状态、可执行操作、需要与相邻内容形成明确分组，或确实需要强调警告时才使用卡片容器；不能为了填充空白重复套卡片。

## 浮层与弹出界面

- 由全宽菜单行触发的浮层默认与触发器等宽，并跟随侧栏尺寸变化；不得使用与锚点无关的固定宽度。
- 浮层标题、操作行和分组标签共享同一内容栅格。存在操作行图标时，标题也提供同尺寸图标，并使用相同字号和横向内缩，使图标列与文字列分别对齐。
- 浮层中的可点击操作在静止状态也必须具有可辨识的背景和边框，不能只依赖悬停状态表达可点击性。
- 悬停、键盘焦点和当前选中状态分别提供清晰反馈，不能互相替代。

## 导航与项目树

- 模型侧栏直接展示项目树，不额外增加重复标题区和空状态大卡片。
- 只有数据量足以产生查找需求时才显示搜索框，不为骨架状态预留无效搜索。
- 新建模型等项目操作放入项目树右键菜单，不在侧栏顶部重复放置按钮。
- 折叠侧栏时必须完全释放其宽度；展开入口移动到剩余内容区域左侧，不能保留空白窄栏。
- 左侧项目树和右侧属性面板使用可拖拽的停靠分隔条调整宽度，并设置最小、最大宽度，不能无限侵占中央视口；用户选择的尺寸保存在本机。
- 停靠面板整体高度跟随工作区，不通过拖拽留下无意义空白。需要调整高度时，在面板内部使用水平分隔条分配上下内容区。
- 分隔条必须提供足够的透明命中区域、清晰的悬停和键盘焦点反馈，并支持方向键微调。
- 从底部菜单触发的浮层左边界与侧栏边界对齐，不额外叠加内容区缩进。
- 可纵向调整的内容区默认高度必须贴合当前内容，不为未来数据预留可见空白；只有用户主动拖拽后才保留额外空间。
- 项目树容器不添加上下内边距，首行和末行直接遵循面板边界；每一行自行提供垂直点击区域。
- 项目树整行悬停和选中背景使用直角，不使用圆角制造额外的卡片感。
- 所有树层级的主标签使用相同字号；层级关系只通过缩进、图标、字重和颜色表达，不通过字号变化表达。

## 顶部工具栏

- 同一顶部工具栏中的图标按钮共享尺寸、边框、背景、圆角和悬停状态；折叠、展开等布局操作不能使用与撤销、重做等工具操作明显不同的视觉体系。
- 主操作可以通过文字和强调背景区分，但控件高度必须与相邻图标按钮一致。

## 状态栏

- 状态栏只展示与当前编辑动作直接相关的信息：当前命令或就绪状态、选择上下文、单位等建模环境，以及需要用户注意的后台服务状态。
- 左侧放操作状态和选择上下文，右侧放环境与服务状态；稳定信息保持位置固定，瞬时提示优先替换左侧操作状态。
- 版本号、端口、是否使用某种协议、部署方式等诊断或架构信息不得占用状态栏，应放入“关于”、诊断页面或智能体文档。
- 未实现鼠标坐标、几何计算进度、自动保存等功能前，不得在状态栏展示伪动态数据。

## 验证

- 修改后运行 `npm run typecheck`、`npm test` 和 `npm run build`。
- 检查 `/llms.txt`、`/capabilities.json`、路径级 `/skill.md` 与 CLI `--llms` 是否仍由同一 capability registry 生成。

## GitHub 多机协作

- 协作提示词只负责启动流程，不复制任务背景、分支名、依赖、Owned paths 或验收标准；这些动态事实必须从仓库和 GitHub 读取。
- 信息优先级为：当前路径适用的 `AGENTS.md` → 叶子 Issue 及其父工作流/总 Issue → `.ai/specs/**` → 当前 PR。提示词与这些来源冲突时，以仓库和 GitHub 实时状态为准。
- 开始工作前运行 `npm run collab:status`。只能认领输出中的 `kind:task + status:ready` 叶子任务；工作流 Issue、Epic、仍有未完成依赖、已有 Assignee 的任务不能直接实现。
- 使用 `npm run collab:claim -- <issue>` 完成认领。命令会再次校验依赖和竞争状态、设置 Assignee/`status:claimed`、从协作配置指定的基线创建并推送独立分支，并把 Owned paths 记录到 Issue。
- 实现范围以叶子 Issue 的 Owned paths 为边界。需要修改 Shared paths 或边界外文件时，先在 Issue 评论说明原因；不得因为提示词提到某个方向就扩大任务范围。
- 完成后提交代码，并使用 `npm run collab:handoff -- <issue> --mobile "<移动端影响>"` 交付。UI、3D 和模型任务还必须传 `--evidence <截图或验收记录>`。命令会运行统一验证、推送/创建 PR，并把 Issue 转为 `status:review`，不会自行标记完成。
- `status:done` 和关闭 Issue 只发生在审查、合入与验收后。视觉原型、独立预览或 planned 占位不能作为完成依据。
- 本轮 Epic、集成基线、验证命令和视觉证据标签位于 `.ai/collaboration/config.json`；本轮结束时更新该配置，不把它变成长期固定分支。
- 完整使用说明和可转发的最小提示词见 `.ai/collaboration/README.md`。
