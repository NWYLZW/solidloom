# 声明式规则与流程契约

本目录提供领域无关、可序列化的表达式 AST、规则效果和长流程状态机契约。表达式只能读取领域包显式声明的字段目录，不支持函数调用、脚本求值、原型链访问或隐式全局变量。

规则效果先被解析为受控命令，再由服务端适配器以原子批次提交。资源账本、实体存储和事件持久化等适配器未接入时，对应效果不能被当作已经执行。

流程运行时使用仿真时间，所有写操作都要求修订号；暂停时间不计入阶段超时。失败和取消按阶段访问的逆序生成补偿效果，并保留审计记录。
